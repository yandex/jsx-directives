{
  "name": "jsx-directives",
  "version": "0.2.0",
  "description": "Directives mechanics for JSX",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc src/index.ts --declaration --outDir dist && cp package.json dist && cp README.md dist",
    "deploy": "cd dist && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yandex/jsx-directives"
  },
  "author": "Alexander Abramov<alabr@yandex-team.ru>",
  "license": "MIT",
  "peerDependencies": {
    "@types/node": "^12.12.16",
    "@types/react": "^16",
    "react": "^16",
    "typescript": "^3.9.6"
  }
}
